# 집직구 작업 가이드

## 제품 원칙
- 모바일 첫 화면은 빠르게 보여준다. 긴 설명보다 버튼, 카드, 상태 배지로 전달한다.
- 핵심 전환은 `직거래`다. 매물 등록, 등록 매물 보기, 단지 내 매물 진입을 우선순위로 둔다.
- 지도는 사용자가 움직일 때 끊기지 않아야 한다. 대량 데이터는 서버 캐시를 쓰고, 화면에는 현재 영역 중심으로만 얹는다.

## 코드 배치
- `src/App.tsx`: 화면 조립, 상태, 서버 호출만 둔다. 새 기능이 커지면 하위 파일로 분리한다.
- `src/listings.ts`: 매물 타입, 매물 라벨, 단지 후보 매칭, 직거래 공통 포맷을 둔다.
- `src/finance.ts`: AI추천 금융 계산과 대출/DSR 관련 계산을 둔다.
- `src/App.css`: 모바일 UI 토큰과 화면 스타일을 둔다. 같은 컴포넌트 변형은 기존 클래스 확장으로 처리한다.

## 직거래 수정 규칙
- 직거래 첫 화면 CTA는 상단 `매물 등록하기` 하나만 둔다. `집 내놓기`, `매물 원해요`, 보조 등록 버튼을 중복 노출하지 않는다.
- 매수희망/매물원해요 흐름은 명시 요청 전까지 비활성으로 둔다.
- 매물 주소는 사용자가 입력한 동 단위 주소보다 `아파트명 -> RTMS 후보 주소 -> Kakao 검색 좌표` 순서로 보정한다.
- 등록 매물 지도 좌표는 저장 시점에 단지 후보 좌표로 보정하고, 지도 렌더에서 임의 위치로 다시 추정하지 않는다.
- 고객 화면에는 `실소유자 검증단계` 또는 `검증완료`만 짧게 보여준다.
- 검증/서류/약관 같은 긴 절차는 등록 마지막 단계와 관리자 확인 흐름에만 둔다.
- 사진은 카드 썸네일, 상세 전체보기, 확대 보기까지 한 흐름으로 유지하고, 깨진 외부/파일명 경로는 브랜드 플레이스홀더로 대체한다.
- 하단 탭은 `직거래 -> 지도 -> AI추천 -> 청약 -> 마이페이지` 순서를 유지한다.
- 리포트는 지도/마이페이지 진입으로만 연결하고 하단 탭을 늘리지 않는다.

## 성능 규칙
- 홈 지도에서 렌더마다 전체 마커를 다시 만들지 않는다.
- 검색어 자동완성은 아파트명만 노출하고, 평형 후보는 숨긴다.
- 좌표 변환은 브라우저 반복 호출보다 서버/Firebase/파일 캐시에 저장하는 방향을 우선한다.

## 배포 전 확인
- `npm run build`
- 모바일에서 홈 지도 이동, 아파트 검색, 매물 카드 클릭, 직거래 등록 버튼을 확인한다.
- Render 환경변수는 배포 서비스와 환경 그룹 양쪽에 빠진 키가 없는지 확인한다.
